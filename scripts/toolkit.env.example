# Example environment overrides for Invoice Ninja Toolkit

# Web server user (default: www-data)
WEB_USER=www-data

# Invoice Ninja application directory (default: /var/www/invoiceninja)
IN_DIR=/var/www/invoiceninja

# Toolkit runtime directory (default: /opt/invoiceninja-toolkit)
TOOLKIT_DIR=/opt/invoiceninja-toolkit

# Database settings for backup/restore (defaults: invoiceninja on localhost, user root)
DB_NAME=invoiceninja
DB_USER=root
DB_HOST=localhost
# DB_EXTRA_OPTS=--password=secret
